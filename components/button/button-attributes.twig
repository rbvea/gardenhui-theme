{#
 # Attributes: Button
 # args:
 #   - href {string} url to link
 #   - classes {string} space seperated classes
 #   - page {integer} page to start off with
 #   - component {string} name of component to load
 #}

href="{{ href }}"
class="{{ classes }}"
